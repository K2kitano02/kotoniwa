<% flash.each do |type, message| %>
  <% next if message.blank? %>
  <% css = type == "notice" ? "bg-green-100 text-green-800" : "bg-red-100 text-red-800" %>
  <div class="flash py-3 px-4 mb-4 text-sm rounded-lg <%= css %>" role="alert">
    <%= message %>
  </div>
<% end %>